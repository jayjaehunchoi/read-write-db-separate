CREATE TABLE member (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL
);

insert into member (name) values ('huni');
insert into member (name) values ('name');
insert into member (name) values ('lala');
